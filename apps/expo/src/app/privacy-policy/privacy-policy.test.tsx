import React from 'react'

import { render, screen } from '@testing-library/react-native'

import PrivacyPolicy from '.'

test('renders policy page and checks text content', () => {
  render(<PrivacyPolicy />)

  
})
