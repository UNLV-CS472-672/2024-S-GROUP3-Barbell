// The Prisma schema is the source of truth for your database schema.
// read the docs, its nice: https://pris.ly/d/prisma-schema

// > ---------------------------------- common commands ----------------------------------

// run 'npx prisma generate' after a schema change to update the prisma client
// run 'npx prisma db push' to push schema changes to planetscale
// run 'npx prisma format' to auto format
// run 'npm run prisma' to run the prisma cli

// > ---------------------------------- schema --------------------------------------------

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

generator client {
  provider = "prisma-client-js"
}

// ******************************************************************************************
// * UserRole Model
// * - This is for the relation between a user and their role.
// * - Where we connect the roleId with the userId.
// * - That way if we want to query all the roles a user has, we query this collection.
// * - Documentation of each model will follow this format. (maybe, I'm making this up as I go)
// ******************************************************************************************
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}